name: first-actions
on: workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print something
        run: echo "hello world from github"

      - name: bye
        run: echo "bye!"